---
title: "FEWSR: examples"
author: "Irucka Embry"
date: "`r Sys.Date()`"
output:
  rmarkdown::html_document:
    highlight: kate
    theme: readable
vignette: >
  %\VignetteIndexEntry{FEWSR: examples}
  %\VignetteEngine{knitr::rmarkdown}
  \usepackage[utf8]{inputenc}
---

<br />
<br />

# Examples
```{r eval = FALSE}
# Example to check the input file format
library(install.load)
load_package("FEWSR", "openxlsx") # load needed packages using the load_package function from the install.load package (it is assumed that you have already installed these packages)


# Copy and paste the following code into the R console if you wish to see the input file format for the Lake plants.
openXL(system.file("extdata", "FEWS_BIG_Lake_plants_input.xlsx", package = "FEWSR"))
  # opens the workbook using the default spreadsheet application


# Copy and paste the following code into the R console if you wish to see the input file format for the Pond plants.
openXL(system.file("extdata", "FEWS_Pond_plants_input.xlsx", package = "FEWSR"))
  # opens the workbook using the default spreadsheet application


# Copy and paste the following code into the R console if you wish to see the input file format for the River plants.
openXL(system.file("extdata", "FEWS_River_plants_input.xlsx", package = "FEWSR"))
  # opens the workbook using the default spreadsheet application

<br />
<br />

# Examples to show you different use cases
fewsr(sheet = "Input", type = "pond", output = "csv")
# The sheet name is "Input" and the surface water is pond


fewsr(type = "river", output = "xlsx")
# The sheet number is 1 (default) and the surface water is river


fewsr(sheet = 4, type = "lake", output = "both")
# The sheet number is 4 and the surface water is lake
```


# References
